model_provider: "openai-compatible"
default_stack: "web_fullstack"
sandbox:
  allow_commands:
    - python
    - pytest
    - node
    - npm
    - git
run_root: "runs"
stack_root: "stacks"
logging:
  level: "INFO"
