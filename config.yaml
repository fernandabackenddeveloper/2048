model_provider: "openai-compatible"
default_stack: "web_fullstack"
run_root: "runs"
stack_root: "stacks"
sandbox:
  allow_commands:
    - python
    - pytest
    - node
    - npm
    - git
logging:
  level: "INFO"
